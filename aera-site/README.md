# AERA — Autonomy, Engineered

Cinematic marketing landing page for **Aera Motor Co.**, built with Vite + React + TypeScript + Tailwind CSS + GSAP.

## Run

```bash
cd aera-site
npm install
npm run dev
```

## Build

```bash
npm run build
npm run preview
```
